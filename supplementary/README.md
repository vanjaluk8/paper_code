# Supplementary Materials — submission bundle

**Manuscript:** *Decentralised Adapter-Based LLM Inference: A Systematic Literature
Review — Mapping the Research Gap at the Intersection of PEFT, P2P Systems, and
Multi-Task Serving* · submitted to *AI* (MDPI) · single author: Vanja Luk,
University of Rijeka.

Everything an MDPI submission needs, in one folder. Files prefixed `S1_`/`S2_`
are the supplementary items the manuscript formally declares (`\supplementary{}`
in `main.tex`). The rest is supporting data referenced from the text or the Data
Availability Statement.

---

## Formal supplementary items (declared in the manuscript)

| ID | File | What it is | Cited in |
|---|---|---|---|
| **S1** | `S1_PRISMA_2020_checklist.md` | Completed PRISMA 2020 27-item checklist: each item mapped to the section / appendix / table where it is addressed. | §3 (`\supplementary{}`); PRISMA-compliance statement in Methods |
| **S2** | `S2_quality_appraisal_scored.csv` | Per-record reporting-completeness & relevance appraisal for all **123** included records: `q1`–`q4`, `q6` (each 0–2), `reporting_completeness_score` (0–10 total), `q5` + `q5_populated` (scored separately), `reporting_completeness_band`. Reproduces §3.4: mean **6.04**, SD **2.00**, median 6; bands 13 / 61 / 47 / 2 (high / upper-mid / lower-mid / low); Q5 populated for 16 records (all = 2). | §3.4, Table "Reporting-completeness and relevance rubric", Figure "Reporting-completeness score distribution" |

## Supporting data (referenced from the text / DAS; not S-numbered)

| File | What it is | Cited in |
|---|---|---|
| `artifact_availability_verified.csv` | The dated, per-row code/artefact-availability check over 33 systems (`verdict`, `evidence_url`, `note`). Backs the §3.4.1 reproducibility finding (79% release usable code). | §3.4.1 "Reproducibility: Artifact Availability" |
| `search_exports/wos_related_surveys_2026-09-10.csv` | Raw Web of Science export (50 records) from the dedicated related-reviews search of 2026-09-10. | §3.1 "Related reviews and surveys"; Appendix C `Related-Reviews Search`; Table "This review against the closest competing secondary studies" |
| `search_exports/scopus_related_surveys_2026-09-10.csv` | Raw Scopus export (19 records, doc-type `re`) from the same search. | as above |
| `PRISMA_summary_2026-04-21.md` | PRISMA funnel summary auto-generated by the pipeline; version-controlled in `mdpi_paper_slr`. | Appendix D; DAS |

## Other files in this folder

| File | Purpose |
|---|---|
| `manuscript_compiled.pdf` | Current compiled manuscript (78 pp, build 2026-09-10). Convenience copy kept **local only** (PDFs are gitignored — not committed); the authoritative build is `../build/out/` via `../build/compile_mdpi.sh`. |
| `cover_letter.md` | Draft cover letter. **Needs updating before submission** — see "Author to-do" below. |

Everything in this folder except `../README.md` and `cover_letter.md` is either a
formal supplementary item or supporting data that a reviewer may ask for.
`S1_PRISMA_2020_checklist.md` here is the submission copy. The earlier working
number-audit table (`PRISMA_NUMBERS_VALIDATION.md`) is **no longer tracked in this
repo** — it was maintained under the old `writing/` layout in git history, and its
authoritative values now live in `mdpi_paper_slr` under `snowball_output/` (per the DAS).

---

## The manuscript package (NOT in this folder — kept as repo source)

The manuscript files live at the **repo root** (the old `writing/mdpi_paper/`
folder was flattened during the reorg). For a LaTeX submission, zip from the repo
root (`/Users/vluk/PycharmProjects/mdpi_paper_slr`):
`main.tex`, `bibliography.bib`, `sections/*.tex`, `figures/*.pdf`,
`Definitions/` (the MDPI class + assets), and `main.pdf`. Do **not** include
`papers_repo/`, `REVISION_STATUS.md`, `VALIDATION_NOTE.md`, `build/`, or anything
under `_archive/` (the remediation working docs, raw search exports, superseded
PRISMA files, figure-generation scripts, and reviewer-response drafts; none of
these currently exist in the flat layout).

## Data Availability — external repositories (per the DAS)

The DAS points to two public GitHub repos and names per-record artefacts
version-controlled in `mdpi_paper_slr`:
`13_final_reading_list_2026-05-12.csv`, `pipeline_unified.csv`,
`log_retrieval_2026-04-21.json`, `log_screening_2026-04-21.json`,
`S7b_abstract_reviewed_final.csv`, `11_data_extraction_2026-05-12.csv`,
`14_final_curation_reasons.csv`, `PRISMA_summary_2026-04-21.md`. The
verification scripts `src/slr_engine/verify/verify_prisma_counts.py` and
`src/slr_engine/verify/verify_quality_appraisal.py` live in `mdpi_slr_engine` (code-only),
and their run outputs are version-controlled in `mdpi_paper_slr`.
Both verify scripts pass (40/40 and 19/19) as of 2026-09-10.

## Author to-do before submission

1. **Cover letter** — regenerate `cover_letter.md`: change "Wohlin et al.
   snowballing" → "a single-wave adaptation of Wohlin et al. snowballing";
   add a sentence declaring that an earlier protocol draft was prepared for a
   doctoral methods course at the University of Rijeka and was **not deposited
   or published** (M18); mention the two supplementary searches of 2026-09-10
   (related surveys + disconfirming/adversarial) and that the eight
   grey-literature query strings are in Appendix C.
2. **Staged (2026-09-11):** the eight per-record artefacts and
   `snowball_output/verification_runs/` (incl. the combined report
   `FINAL_REPORT_2026-09-11.md/json` — 78/78 checks pass — and the archived
   `FINAL_REPORT_2026-09-02.md/json`, plus the `verify_*.result.txt`) are now
   committed under `snowball_output/` here, and `14_final_curation_reasons.csv`
   is present. **`mdpi_slr_engine`** — the two `verify_*.py` scripts live under
   `src/slr_engine/verify/` (code-only), which is already committed.
3. **`mdpi_paper_slr`** — make the repository public.
4. **S1 format** — if the editor requires the official PRISMA 2020 checklist
   PDF/DOCX rather than this mapping table, fill the template at
   <https://www.prisma-statement.org> using the section pointers in
   `S1_PRISMA_2020_checklist.md`.
5. **ORCID** — present as the iD icon in the title block (links to
   `https://orcid.org/0009-0009-8142-6890`); also enter it in the MDPI
   submission-system metadata field.
6. Run iThenticate pre-check.
